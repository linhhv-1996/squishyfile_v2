// Routes that belong to the "Video" tool section, so the header/footer can
// highlight the right nav item regardless of which of the (currently
// identical) tool pages is active.
export const videoRoutes = ['/', '/compress-video-to-size', '/compress-video-on-iphone'];

// Same idea for the video-to-MP3 tool section (hub page + the two
// format-specific pages).
export const mp3Routes = ['/video-to-mp3', '/mp4-to-mp3', '/mov-to-mp3'];
